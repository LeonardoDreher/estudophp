<?php
print 'todo o conteúdo está comentado';
/* Operador de incremento o ++
$valor = 100;
$valor = $valor + 5;  //Utilizado para usar a variavel para incrementar um valor 
$valor ++;    //Usando o operador de incremento (++) para adicionar 1 número
$valor += 5;  //incrementa em x unidades
$valor -= 5;  //decrementa em x unidades
$valor *= 5;  //multiplica a mesma variavel em x vezes
$valor /= 5;  //divide a mesma variavel em x vezes 

//outro teste
$teste = $valor ++;  //quando (++) está na direita a operação primeiro retorna o próprio conteúdo da variável
$teste = ++ $valor;  //quando (++) está na esquerda ele primeiro incrementa e depois retorna a variável

var_dump ($valor);
echo '<br>';
var_dump ($teste);
*/

/*Operadores aritméticos*/
//(+ = soma) (- = subtração ) (* = multiplicação) (/ = divisão)

//$a = 2;
//$b = 4;
//echo ($a+3) *4 + (5*$b);

/*Operadores de comparação*/ 
// (>  maior) (<  menor) (>=  maior igual) (<=  menor igual) (!==  diferente) (== comparação) (=== comparação que compara o tipo)
/*
$valor1 = 100;
$valor2 = '100';

if ($valor1 == $valor2){ // nunca usar ( = ) pois ele é usado para atribuição, ou seja, o valor2 será atribuido ao valor1
    echo 'são iguais';
}  */

/*Operadores lógicos*/
/*
$salario = 1000;
$cargo = 'gerente';


// && ou AND podem ser usados para se referir a (e) ex: (paulo AND julio) ou (paulo && julio) Basicamente se paulo e julio estiverem certo o if é verdadeiro
if ($salario > 800 && $cargo == 'gerente'){ 
    echo 'ok';
}

// || ou OR podem ser usados para se referir a (ou) ex: (paulo || julio) ou (paulo OR julio) Basicamente se existir paulou ou julio o if é verdadeiro
echo 'ok';
if ($salario > 800 || $cargo == 'gerente'){ 
} */